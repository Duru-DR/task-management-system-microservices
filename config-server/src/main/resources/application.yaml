server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: git@github.com:Duru-DR/task_management_system_config_repo.git
          clone-on-start: true
          default-label: main
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_TOKEN}
